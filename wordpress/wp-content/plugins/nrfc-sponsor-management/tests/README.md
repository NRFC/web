# NRFC Sponsor Management Unit Tests

Install dev dependencies and run tests from this plugin directory:

```bash
composer install
composer test
```

The tests use lightweight WordPress function stubs in `tests/bootstrap.php` so they can run without a full WordPress bootstrap.

